name: environmentVar

on:
  workflow_dispatch:

env:
  app: arif

jobs:
  Env:
    runs-on: ubuntu-latest

    steps:
      - name: env-step
        run: |
          echo "I am displaying my name: $app"
